window.CHANGELOG_MD = "# Changelog\n\nAll notable changes to the Worklog Hub project are documented in this file.\n\nThe format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)\nand this project adheres to [Semantic Versioning](https://semver.org/).\n\n## [1.6.0] – Coming soon\n\n### Added\n- **\"My issues\" dashboard** - a header button opens a drawer listing all open issues assigned to you across every configured Jira instance, with per-row **Open detail** and **Create worklog** actions.\n- **Original-estimate warning in the worklog table** - a warning icon next to *Logged* when an issue's total logged time in the period exceeds its Jira original estimate.\n- **Setting \"Sync new worklogs with Jira immediately\"** - choose whether new entries go straight to Jira or stay as local drafts.\n- **Per-entry \"Send to Jira\"** for local worklogs - from the entry dialog, the calendar context menu, a cloud icon in the week view, and the table row menu.\n- **Local AI for worklog comment drafting and calendar matching** - opt-in and fully local: it runs in your browser via WebGPU, so no worklog or ticket data leaves your device. Enable it in **Settings → General → Use local AI**; the model downloads once (~2.5 GB) into the browser cache and then runs offline.\n  - **Comment drafting** - the ✨ button in the entry detail proposes a worklog comment from the ticket context (a few seconds on capable hardware).\n  - **Semantic calendar matching** - re-ranks low-confidence Jira matches for calendar meetings.\n  - **Slow-hardware guard** - on enable the app measures the GPU's decode speed (with a \"Measuring speed…\" spinner); on a slow GPU it shows a warning and turns AI back off, since generating a note would take minutes.\n  - **\"Delete model\"** in Settings frees the cached model (~2.5 GB); it re-downloads on next use.\n\n### Changed\n- **Day overview** - a chip with the selected day now sits above the table; its × returns to the whole-month view.\n- **Removed the \"Sync this entry immediately\" checkbox** from the entry dialog - the new global setting governs the default and per-entry sending lives in the menus.\n- **Switches use the brand colour when checked** instead of green (which read as \"success\" on every toggle).\n- Minor spacing/typography polish in Settings.\n\n### Fixed\n- **Ctrl+drag duplicate in the month calendar** - no more event ghost or dimming; the duplicate lands below existing entries on the target day.\n- General-section boxes in Settings no longer overflow their column.\n- The login-required snackbar action button is now readable on the warning toast.\n- The time-spent picker no longer clears itself mid-typing.\n\n[1.6.0]: https://github.com/lgorzocz/workloghub/releases/tag/v1.6.0\n\n## [1.5.0] – 2026-05-19\n\n### Added\n- **Drag & drop in the week list view**, matching the month view - plain drag moves an entry, Ctrl+drag duplicates it.\n- **Calendar integration via private `.ics` feeds** - detect meetings without a worklog. Configure Google / Outlook / iCloud sources in **Settings → Calendar suggestions** (per-source toggle, URL whitelist, per-origin permission).\n- **Calendar scanner** matches each meeting to a Jira issue at three confidence tiers - explicit key, project prefix, or fulltext - using the same lookback windows as the comment scanner.\n- **Placeholder filtering** across 9 locales: soft-skip (Busy/Tentative are kept as separate unmatched suggestions), hard-skip (Free/OOO/Vacation/Holiday are ignored).\n- **Unified suggestions dialog** - one list (comments + matched + unmatched calendar) grouped by date, replacing the two previous dialogs.\n- **Manual ticket assignment** on calendar rows - assign unmatched meetings or re-assign wrong matches via an inline issue autocompleter seeded with candidates.\n\n### Changed\n- \"Sync this entry immediately\" now defaults to **unchecked when editing an existing local entry** (new entries and duplicates still default to checked).\n- Calendar-source help rewritten against the real 2026 Google/Outlook/iCloud UI.\n\n[1.5.0]: https://github.com/lgorzocz/workloghub/releases/tag/v1.5.0\n\n## [1.4.0] – 2026-05-17\n\n### Added\n- **Fulltext issue search in the entry dialog** - searches summary, description and comments across all instances (from 3 characters), so you no longer need to know the project prefix; each result shows a coloured instance chip.\n- **Un-logged worklog suggestions from Jira comments** - opt-in via **Settings → \"Suggest worklogs from comments\"**. Daily auto-scan (Monday backfills Fri–Sun) plus a manual lightbulb scan of the current month; *Save* creates a 5-min draft, *Forget* dismisses permanently.\n- **Duplicate worklog entry** - from the table row menu, the entry-dialog kebab, and calendar right-click; opens a pre-filled editor without a date so you pick the target day.\n- **Drag & drop duplicate** - Ctrl/Cmd+drag duplicates an entry onto the target day (plain drag still moves it).\n- **Bulk worklog in the standard create dialog** - Ctrl/Cmd+click days (or add dates in the dialog) to log the same time on multiple days at once; saved serially so one failure doesn't abort the batch.\n- **Reports - \"Average per day\" card** (hours per working day from period start through today; weekends and future days excluded).\n- **CSV import accepts both `,` and `;` separators** (auto-detected) - fixes EU-locale Excel exports that were previously rejected.\n\n### Changed\n- **Removed the manual \"Issue prefixes\" field** - worklog routing is now automatic; the prefix list became a read-only, auto-accumulated cache (it frequently collided when the same prefix existed in multiple instances).\n- **Unified issue search across instances** picks the JQL by input shape (full key / `PRJ-` prefix / single word / phrase); default per-instance limit raised 10 → 25.\n- **CSV import auto-resolves the target instance per row** (local map → prefix cache → fulltext fallback).\n- **MUI Date Picker UI strings** (OK / Cancel / Today / a11y labels) now follow the active app language.\n- **Entry editor is now a centered Dialog** instead of a right-side Drawer; it hosts both single- and bulk-day creation.\n- **Report export confirmation also warns when there are unsynced local entries** (the report is built from Jira data only), combined with the \"month not ended\" warning into a single dialog.\n- **Reports - Projects and Instances cards merged** into one \"Projects / Instances\" card.\n\n### Fixed\n- Date pickers showed Czech month names for `es` / `fr` / `it` - registered the missing `date-fns` locales.\n- Stale *Forget*-ed suggestions in `localStorage` are now pruned (entries older than the current month).\n- Bulk / local worklog no longer lost all but the last entry - mutators read current storage instead of a stale render snapshot.\n\n[1.4.0]: https://github.com/lgorzocz/workloghub/releases/tag/v1.4.0\n\n## [1.2.0] – 2026-05-15\n\n### Added\n- Localization for Spanish (`es`), French (`fr`) and Italian (`it`) - UI and Help.\n- Store-listing localization across 8 languages via `_locales` + `__MSG_*__` manifest placeholders (`default_locale: \"en\"` fallback).\n\n### Changed\n- CSV import hardened - per-row `try/catch` so one bad row doesn't abort the batch; the final notification reports imported / total / skipped accurately.\n- Unified input and autocomplete-option font sizes via the theme; refreshed Help (cs/de/en/pl/sk) and store materials.\n\n### Fixed\n- \"Import from CSV\" is now disabled when no Jira instance is configured (previously it silently skipped every row yet reported success).\n- Manifest no longer requests the `storage` permission (the app uses `localStorage`, not `chrome.storage`), reducing the install-time permission warning.\n- Minor behaviour and styling fixes in Worklog and FilterLine.\n\n[1.2.0]: https://github.com/lgorzocz/workloghub/releases/tag/v1.2.0\n";
